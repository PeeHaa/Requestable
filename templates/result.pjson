<?php

$result = [
    'headers' => null,
    'body'    => null,
    'error'   => null,
];

if (isset($error)) {
    $result['error'] = $error;
} else {
    $result['headers'] = $requestInfo['headers'];
    $result['body']    = $requestInfo['body'];
}

echo json_encode($result);
