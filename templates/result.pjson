<?php

$result = [
    'headers' => null,
    'body'    => null,
    'error'   => null,
];

if (isset($error)) {
    $result['error'] = $error;
} else {
    $result['headers'] = $requestInfo['header'];
    $result['body']    = $requestInfo['body'];
}

echo json_encode($result);
